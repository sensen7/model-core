# 复制本文件为 .env，填写真实值后执行 docker compose up -d
# 注意：.env 文件包含敏感信息，不要提交到 Git

# 数据库密码
DB_USERNAME=root
DB_PASSWORD=your_db_password_here

# Redis 密码
REDIS_PASSWORD=your_redis_password_here

# JWT 密钥（建议使用随机生成的 64 位以上字符串）
JWT_SECRET=your_jwt_secret_at_least_256_bits_long_replace_this
